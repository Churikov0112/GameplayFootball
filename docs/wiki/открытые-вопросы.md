# Открытые вопросы

Что не подтверждено, что отложено и почему. Закрытый пункт — **удаляется**, а не помечается
сделанным. Веха из закрытого пункта — запись в `log.md`.

## Не подтверждено

- macOS: сборка проходит, но **не запускается** — рендеринг обязан идти в main thread, а в
  `GraphicsSystem::Initialize` рендерер стартует в отдельном потоке (`OpenGLRenderer3D::Run`,
  `src/systems/graphics/graphics_system.cpp:34-39`). Решение не найдено, README честно помечает
  как Work in Progress.
- **CI не запускался** (ветка `build-modernization` не запушена) — workflow `.github/workflows/build.yml`
  валиден и x86-эталон подтверждён локально, но первый прогон GitHub Actions ещё не состоялся.

## Эталоны детерминизма на платформу (Task 6)

- **Linux — TODO.** `tools/determinism/reference-linux.txt` не снят: локально нет Linux-хоста,
  а хэш можно получить только на Linux. Первый запуск `linux`-job в CI сгенерирует файл
  (workflow сам пишет его при отсутствии) — после прогона хэш проверить и закоммитить.
  Linux-job привязан к `ubuntu-26.04`: apt-пакеты SDL3 (`libsdl3-dev` и др.) появились только
  в Ubuntu 26.04 LTS, а `ubuntu-latest` до сих пор указывает на 24.04.
- **Windows x64 — снят локально, требует сверки с CI.** `tools/determinism/reference-windows-x64.txt`
  = `4e9ddb725d79013040e65df7dcf4aa16026bb55d` (снят на MSVC 2022, x64, локальная машина).
  Хэш отличается от x86 (`372c4bbd...`), т.е. эталон действительно привязан к платформе.
  CI (windows-latest, VS2026) может дать другой хэш — `windows-x64`-job работает в режиме
  «capture»: на первом прогоне перезапишет файл и поднимет warning при расхождении; новое
  значение закоммитить (Task 6).

## Отложено осознанно

- **Физика (ODE)** не компилируется для игры («not compiling physics, as not used by
  gameplayfootball», `CMakeLists.txt:59`). Симуляция мяча/касаний — ручная. Если вернуть физику —
  сначала прочитать `docs/wiki/матч` и согласовать буферы Get/Process/Put.
- **`dataSetSortable`** (`src/gamedefines.hpp:70`) закомментирован: переключает `DataSet`
  между std::list и std::deque. Код собран и проверен только с `std::deque`.
- **Ветки `google_brain` и `windows`** — чужие линии с незавершённой работой; не вливать целиком.
- **Тестов нет** — единственная проверка компиляцией (`cmake --build`). Частично закрыто:
  появился детерминированный headless-раннер `tools/determinism` с эталоном
  `reference.txt` (регрессионная проверка «геймплей не изменился»), но это не полноценные
  юнит-тесты.

## Отвергнутые направления

- **Вендорный SQLite** — убран из репозитория в `68159a2`, сборка использует системный пакет.
- **Glew** — удалён в `ac722a9`, обвязка вычищена; не возвращать без необходимости.
- **SGE и старый SDL 1.2** — заменены на SDL2 в `ac15330`.
